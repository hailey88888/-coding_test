class Solution {
    public int[] solution(String[] strlist) {
        int[] answer = new int[strlist.length] ;
        
        for(int i =0 ; i<strlist.length; i++){
            answer[i]=strlist[i].length();
        }
        return answer;
    }
}


"int[] answer = new int[strlist.length]" 줄은 입력 문자열 배열 "strlist"의 길이와 동일한 길이를 가진 "answer"라는 정수 배열을 만듭니다.

이 정수 배열을 만드는 이유는 함수가 정수 배열을 반환하도록 설계되었기 때문입니다. 여기서 반환된 배열의 각 요소는 입력 문자열 배열의 문자열 길이를 나타냅니다.

int[] answer = {};이렇게 하면 제공된 코드는 정수 배열 "answer"가 어떤 크기로도 초기화되지 않았고 빈 배열은 값을 저장할 수 없기 때문에 런타임 오류가 발생
